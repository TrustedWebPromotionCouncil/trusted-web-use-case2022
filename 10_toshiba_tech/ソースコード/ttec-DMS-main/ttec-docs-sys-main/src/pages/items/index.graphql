mutation RemoveDocument ($documentId: String!) {
    removeDocument(documentId: $documentId)
}

query GetDocuments {
    documents {
        id
        location
        filename
        scanedAt
    }
}