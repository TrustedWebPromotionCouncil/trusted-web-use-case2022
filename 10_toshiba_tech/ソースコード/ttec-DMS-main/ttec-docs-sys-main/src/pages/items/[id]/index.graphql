query GetDocument($documentId: String!) {
    document(documentId: $documentId) {
        id
        did
        vc
        location
        username
        serialNumber
        scanedAt
        filename
        mimeType
        data
        createdAt
    }
}